{
  "name": "@astradot-private/kafka-config-generator",
  "version": "1.2.0",
  "description": "kafka",
  "main": "index.js",
  "scripts": {
    "test": "tsc && node dist/app.js --config test-config.yaml --kdir ~/tmpcode/gen-test",
    "start": "tsc && node dist/app.js --help",
    "clean": "rm -rf dist/",
    "compile": "rm -rf dist/ && tsc",
    "doPublish": "npm run compile && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@bitbucket.org/astradot/kafka-config-generator.git"
  },
  "engines": {
    "node": ">=0.14"
  },
  "bin": {
    "kafka-conf-gen": "./dist/app.js"
  },
  "files": [
    "dist"
  ],
  "author": "pdeva",
  "license": "UNLICENSED",
  "homepage": "https://bitbucket.org/astradot/kafka-config-generator#readme",
  "devDependencies": {
    "typescript": "4.0.5"
  },
  "dependencies": {
    "@types/chalk": "^2.2.0",
    "@types/js-yaml": "^3.12.5",
    "@types/node": "^14.14.6",
    "@types/yargs": "^15.0.9",
    "chalk": "^4.1.0",
    "dedent-js": "^1.0.1",
    "js-yaml": "^3.14.0",
    "yargs": "^16.1.0"
  }
}
